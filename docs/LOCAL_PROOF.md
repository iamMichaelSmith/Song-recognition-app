# Local Proof

## Verification completed in this workspace

- `python .\tests\smoke.py` ✅
- Static app structure confirmed (`index.html`, `app.js`, `server.py`, `config.js`, `config.example.js` present)
- Smoke check confirms the shipped app no longer contains a hardcoded AudD API token

## Notes for employers

This repo is intentionally lightweight: it is a browser-first prototype rather than a full packaged backend application. The right way to present it is as a focused utility app that demonstrates:

- user-friendly batch-audio workflow thinking
- practical frontend state handling
- simple local serving / no-heavy-infra setup

For a stronger portfolio read, pair this with screenshots and a short demo clip rather than overselling it as a production system.
